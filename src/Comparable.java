/**
 * Interface allowing objects whose classes implement
 * it to compare themselves based on their properties.
 *
 * @author Tomas Kaljevic
 */
public interface Comparable {
    int compareTo(Object o);
}
